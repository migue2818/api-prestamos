[project]
name = "api-prestamos"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { email = "miguelangelordonez70@gmail.com" }
]
requires-python = ">=3.13"
dependencies = []

[project.scripts]
api-prestamos = "api_prestamos:main"

[build-system]
requires = ["uv_build>=0.12.2,<0.13.0"]
build-backend = "uv_build"
